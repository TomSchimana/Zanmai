[← Zanmai Documentation](index.md)

# Page title

<!--
This is the shape every help page follows, so the documentation reads as one thing.
Keep it: as little as possible, as much as necessary, no filler.

Rules:
- First line is the back-link to the index (round-trip navigation). For a page
  in a sub-folder (e.g. install/), use `../index.md` instead of `index.md`.
- Then the H1 title, then one plain sentence saying what the page is and who
  needs it. Assume the reader is NOT technical, explain the basics, link out.
- Use standard relative Markdown links, never `[[wikilinks]]` (GitHub does not
  render those outside its separate Wiki).
- End with the footer back-link below so every page returns to the index.
- List the page in `index.md` under the right section when you add it.
-->

One sentence: what this page covers and who it is for.

## A section

Plain, short prose or steps. Concrete links where the reader has to act.

---

[← Back to the documentation index](index.md)
