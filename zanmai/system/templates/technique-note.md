# Technique, <tool>

Runtime field notes for one tool, learned in this space and kept because a shipped
skill cannot hold what only a real run reveals. User-immune, updates never touch
it. Curated overriding: a corrected entry replaces the wrong one, it does not
accrete beside it. A mixed "verified" pile with no dates and no confidence is what
poisoned the old notes; this file is the opposite.

Each entry carries a **date + tool version** and a **confidence**:
- `verified <version>`, checked against a real result in this version, it held.
- `observed once`, seen once, not re-confirmed.
- `uncertain`, reported but contested; do not act on it without a check.

**Perishable primitives are self-checked, not trusted.** Anything version-fragile
(corner rounding, font embedding, an export preset) is re-checked against a quick
test render at the start of a run, the entry names *what* to check, the render
decides. Contradictory entries are resolved by the test, never by the newer note
winning by default.

## Entries
- `<date · tool version>`, <one concrete idiom or stumble>, `verified|observed once|uncertain`
